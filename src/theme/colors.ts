const lightColors = {
  grey: {
    100: "#000",
    200: "#010010",
  },
  red: {
    100: "4r00340",
  },
};

const darkColors = {};

export const colors = "dark" === "dark" ? darkColors : lightColors;
